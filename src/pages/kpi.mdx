import { CodeGroup, Row, Col, Properties, Property } from "../components/mdx.jsx"

export const description =
    'On this page, we’ll dive into the different submisson deadline endpoints you can use to manage submission deadline.'


Submisson deadline

## Create Deadline {{ tag: 'POST', label: '/v1/human-resource/submissionDeadline' }}

<Row>
    <Col>
    This endpoint create setting deadline.

    # Request

    ### Required attributes

     <Properties>
        <Property name="year" type="string">
            The name for the year unique
        </Property>
        <Property name="periods" type="array<string>">
            The array of allowed periods month and date submission_deadline.
        </Property>
    </Properties>

     ### Response

    <Properties>
        <Property name="success" type="string">
            The message Successfully setting deadline
        </Property>
    </Properties>

      <Properties>
        <Property name="error" type="string">
            The message Error years is not empty
        </Property>
    </Properties>

    </Col>

    <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/human-resource/submissionDeadline">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/human-resource/submissionDeadline/{years}', {
        year: 2024,
        periods: [
            {
            month: "January",
            submission_deadline: "2024-01-31"
            },
            {
            month: "February",
            submission_deadline: "2024-02-29"
            },
            {
            month: "March",
            submission_deadline: "2024-03-31"
            }
            // Tambahkan bulan lainnya sesuai kebutuhan
        ]
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
        {
            "message": "Succesfuly setting deadline ."
        }
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '404' }}
        {
            "message": "year is not empty and already exist."
        }
    ```
    </CodeGroup>
  </Col>
</Row>

## List get tahun assessment dan periode. {{ tag: 'GET', label: '/v1/human-resource/submissionDeadline' }}

<Row>
  <Col>

    This endpoint get a list of all assessment years and periods.

    # Request

    ### Optional attributes

        <Properties>
            <Property name="year" type="string">
                Unique this year submission deadline
            </Property>
            <Property name="periods" type="array<string>">
                The array of periods month, submission_deadline.
            </Property>
        </Properties>

    # Response 

        <Properties>
             <Property name="year" type="string">
                Get this year submission deadline
            </Property>
        </Properties>
         <Properties>
             <Property name="periods" type="string<array>">
                The array of periods mont submission deadline
            </Property>
        </Properties>
         <Properties>
             <Property name="periods.month" type="string">
                Get name month in array periods
            </Property>
        </Properties>
        <Properties>
             <Property name="periods.submission_deadline" type="date">
                Get date submission_deadline in array periods
            </Property>
        </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/human-resource/submissionDeadline">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/human-resource/submissionDeadline')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
        {
            "year": 2024,
            "periods": [
            {
                "month": "January",
                "submission_deadline": "2024-01-31"
            },
            {
                "month": "February",
                "submission_deadline": "2024-02-28"
            },
            {
                "month": "Maret",
                "submission_deadline": "2024-12-31"
            }
            ...
            ]
        },
        ...
    ```
    </CodeGroup>
  </Col>
</Row>
<br />
## List pertahun assessment dan periode. {{ tag: 'GET', label: '/v1/human-resource/submissionDeadline/{year}' }}
<Row>
  <Col>
  
    This endpoint takes a list of specific years to display data with year parameter.

    # Request

    ### Optional attributes

    <Properties>
            <Property name="year" type="string">
                Unique this year submission deadline
            </Property>
            <Property name="periods" type="array<string>">
                The array of periods month, submission_deadline.
            </Property>
    </Properties>

    # Response 

        <Properties>
             <Property name="year" type="string">
                Get this year submission deadline
            </Property>
        </Properties>
         <Properties>
             <Property name="periods" type="string<array>">
                The array of periods month submission deadline
            </Property>
        </Properties>
         <Properties>
             <Property name="periods.month" type="string">
                Get name month in array periods
            </Property>
        </Properties>
        <Properties>
             <Property name="periods.submission_deadline" type="date">
                Get date submission_deadline in array periods
            </Property>
        </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/human-resource/submissionDeadline/{year}">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/human-resource/submissionDeadline/{years}')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
        {
            "year": 2024,
            "periods": [
            {
                "month": "January",
                "submission_deadline": "2024-01-31"
            },
            {
                "month": "February",
                "submission_deadline": "2024-02-28"
            },
            {
                "month": "Maret",
                "submission_deadline": "2024-12-31"
            }
            ...
            ]
        }
    ```
    </CodeGroup>
  </Col>
</Row>
<br />
## Update setting deadline setiap tahun. {{ tag: 'PATCH', label: '/v1/human-resource/submissionDeadline/:id' }}
<Row>
    <Col>

    Endpoint for setting submission deadline.

    # Request

    ### Required attributes

     <Properties>
        <Property name="year" type="string">
            The name for the year unique
        </Property>
        <Property name="periods" type="array<string>">
            The array of allowed periods name.
        </Property>
    </Properties>

    ### Response

    <Properties>
        <Property name="success" type="string">
            The message Successfully
        </Property>
    </Properties>

      <Properties>
        <Property name="error" type="string">
            The message Error years already exists
        </Property>
    </Properties>
    

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/human-resource/submissionDeadline/:id">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/human-resource/submissionDeadline/:id', {
        year: 2024,
        created_at : 2024-01-31,
        periods: [
            {
            month: "January",
            submission_deadline: "2024-01-31"
            },
            {
            month: "February",
            submission_deadline: "2024-02-29"
            },
            {
            month: "March",
            submission_deadline: "2024-03-31"
            }
            // Tambahkan bulan lainnya sesuai kebutuhan
        ]
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
        {
            "message": "Successfully updated."
        }
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '404' }}
        {
            "message": "Error years already exists."
        }
    ```
    </CodeGroup>
  </Col>
</Row>
<br />
## Delete Deadline . {{ tag: 'DELETE', label: '/v1/human-resource/submissionDeadline/:id' }}
<Row>
    <Col>

    Endpoint for delete deadline.


     <Properties>
           This endpoint allows you to delete from deadline list.
    </Properties>

    

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/human-resource/submissionDeadline/:id">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/human-resource/submissionDeadline/:id', {
        year: 2024,
        periods: [
            {
            month: "January",
            submission_deadline: "2024-01-31"
            },
            {
            month: "February",
            submission_deadline: "2024-02-29"
            },
            {
            month: "March",
            submission_deadline: "2024-03-31"
            }
            // Tambahkan bulan lainnya sesuai kebutuhan
        ]
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
        {
            "message": "Successfully delete form ."
        }
    ```
    </CodeGroup>
  </Col>
</Row>
<br />
## Post Generate Denda . {{ tag: 'POST', label: '/v1/human-resource/generateDenda' }}
<Row>
    <Col>

    Endpoint for generate denda.


     <Properties>
           This endpoint for generate denda.
    </Properties>

    # Request

    ### Required attributes

     <Properties>
        <Property name="employee_id" type="string">
           Unique identifier for the employee.
        </Property>
        <Property name="submission_date" type="string (YYYY-MM-DD)">
            The submission_date The date when the employee submitted their evaluation.
        </Property>
        <Property name="deadline_date" type="string (YYYY-MM-DD)">
            The deadline_date The deadline for submitting evaluations.
        </Property>
        <Property name="status" type="string (draft, submitted)">
            The statys drafy and submitted The status of the evaluation (draft or submitted).
        </Property>
        <Property name="penalty_basis" type="number">
            The penalty_basis The monetary basis for calculating the penalty per day..
        </Property>
    </Properties>

    ### Response

    <Properties>
        <Property name="employee_id" type="string">
           The ID of the employee.
        </Property>
        <Property name="penalty_amount" type="number">
            Total penalty amount in Rupiah.
        </Property>
        <Property name="days_late" type="number">
            Total number of days late.
        </Property>
        <Property name="evaluation_status" type="string">
            Status of the evaluation (submitted, draft, not submitted)..
        </Property>
        <Property name="message" type="string">
            Additional information about the penalty calculation.
        </Property>
    </Properties>

    ### Notes
    <Properties>
        <Property name="note">
           If the penalty basis is not provided or is null, the penalty amount will also be considered null.
        </Property>
        <Property name="note">
            The current date should be dynamically fetched from the server at the time of the request to ensure accuracy.
        </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/human-resource/generateDenda">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/human-resource/generateDenda', {
            employee_id: "E123",
            submission_date: "2023-09-05",
            deadline_date: "2023-09-01",
            status: "draft",
            penalty_basis: 10000
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
        {
            "employee_id": "E123",
            "penalty_amount": 40000,
            "days_late": 4,
            "evaluation_status": "draft",
            "message": "Late submission penalty calculated."
        }
    ```
    </CodeGroup>
  </Col>
</Row>
<br />
## All Report Denda . {{ tag: 'GET', label: '/v1/human-resource/reportDenda' }}
<Row>
    <Col>

    Endpoint for All Report Denda.


     <Properties>
           Endpoint for All Report Denda.
    </Properties>

    ### Response

    <Properties>
        <Property name="Description">
           Retrieves a report of late evaluation penalties for employees.
        </Property>
    </Properties>

    ### Notes
    <Properties>
        <Property name="note">
           The report will include all employees.
        </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/human-resource/reportDenda">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/human-resource/reportDenda')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
        "report": [
                {
                    "employee_id": "E123",
                    "employee_name": "John Doe",
                    "submission_date": "2023-09-05",
                    "deadline_date": "2023-09-01",
                    "days_late": 4,
                    "penalty_amount": 40000,
                    "evaluation_status": "draft"
                },
                {
                    "employee_id": "E456",
                    "employee_name": "Jane Smith",
                    "submission_date": "2023-09-03",
                    "deadline_date": "2023-09-01",
                    "days_late": 2,
                    "penalty_amount": 20000,
                    "evaluation_status": "submitted"
                }
        ],
        "message": "Report generated successfully."
    }
    ```
    </CodeGroup>
  </Col>
</Row>
<br />
## Report Denda Get Range Date . {{ tag: 'GET', label: '/v1/human-resource/reportDenda?start_date=2023-09-01&end_date=2023-09-05' }}
<Row>
    <Col>

    Endpoint for generate denda.


     <Properties>
           This endpoint for generate denda.
    </Properties>

    ### Response

    <Properties>
        <Property name="Description">
           Retrieves a report of late evaluation penalties for employees.
        </Property>
        <Property name="Query Parameters">
            start_date (string, optional): Filter report from this date (YYYY-MM-DD).
        </Property>
        <Property name="Query Parameters">
            end_date (string, optional): Filter report until this date (YYYY-MM-DD).
        </Property>
    </Properties>

    ### Notes
    <Properties>
        <Property name="note">
           The report will include all employees who have late submissions within the specified date range.
        </Property>
        <Property name="note">
            If no parameters are provided, the report will default to showing all penalties for the current month.
        </Property>
        <Property name="note">
            The API should handle cases where no penalties are found, returning an appropriate message and an empty report array.
        </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/human-resource/reportDenda?start_date=2023-09-01&end_date=2023-09-05">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/human-resource/reportDenda?start_date=2023-09-01&end_date=2023-09-05')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
        "report": [
                {
                    "employee_id": "E123",
                    "employee_name": "John Doe",
                    "submission_date": "2023-09-05",
                    "deadline_date": "2023-09-01",
                    "days_late": 4,
                    "penalty_amount": 40000,
                    "evaluation_status": "draft"
                },
                {
                    "employee_id": "E456",
                    "employee_name": "Jane Smith",
                    "submission_date": "2023-09-03",
                    "deadline_date": "2023-09-01",
                    "days_late": 2,
                    "penalty_amount": 20000,
                    "evaluation_status": "submitted"
                }
        ],
        "message": "Report generated successfully."
    }
    ```
    </CodeGroup>
  </Col>
</Row>
<br />
## History Denda. {{ tag: 'GET', label: '/v1/human-resource/historyDenda?employee_id=E123&start_date=2023-08-01&end_date=2023-09-05' }}
<Row>
    <Col>

    Endpoint for histoy denda.


     <Properties>
           This endpoint for histoy denda.
    </Properties>

    ### Response

    <Properties>
        <Property name="Description">
           Retrieves a history of late evaluation penalties for employees, with default filters applied.
        </Property>
        <Property name="Query Parameters">
           employee_id (string, optional): Filter by specific employee ID.
        </Property>
        <Property name="Query Parameters">
            status (string, optional): Filter by evaluation status (e.g., draft, submitted, not submitted).
        </Property>
        <Property name="Query Parameters">
            start_date (string, optional): Filter report from this date (YYYY-MM-DD).
        </Property>
        <Property name="Query Parameters">
            end_date (string, optional): Filter report until this date (YYYY-MM-DD).
        </Property>
        <Property name="Default Filters">
            If no filters are provided, the API will default to returning penalties for the last 30 days.
        </Property>
        <Property name="Default Filters">
            The default status filter will be set to include all statuses.
        </Property>
    </Properties>

    ### Notes
    <Properties>
        <Property name="note">
           If no penalties are found for the specified filters, the response should contain an empty history array and an appropriate message.
        </Property>
        <Property name="note">
            The API should handle date validation and return errors for invalid date formats.
        </Property>
        <Property name="note">
            Pagination may be implemented if the history data set is extensive, to improve performance.
        </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/human-resource/historyDenda?employee_id=E123&start_date=2023-08-01&end_date=2023-09-05">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/human-resource/historyDenda?employee_id=E123&start_date=2023-08-01&end_date=2023-09-05')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
        "history": [
            {
                "employee_id": "E123",
                "employee_name": "John Doe",
                "submission_date": "2023-09-05",
                "deadline_date": "2023-09-01",
                "days_late": 4,
                "penalty_amount": 40000,
                "evaluation_status": "draft",
                "penalty_date": "2023-09-06"
            },
            {
                "employee_id": "E123",
                "employee_name": "John Doe",
                "submission_date": "2023-08-28",
                "deadline_date": "2023-08-25",
                "days_late": 3,
                "penalty_amount": 30000,
                "evaluation_status": "submitted",
                "penalty_date": "2023-08-29"
            }
        ],
        "total_penalties": 70000,
        "message": "Penalty history retrieved successfully."
    }
    ```
    </CodeGroup>
  </Col>
</Row>